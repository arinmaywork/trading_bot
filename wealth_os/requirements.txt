# Wealth OS — lean runtime (no ML/tick stack needed)
aiohttp>=3.9.0
casparser==0.8.1
pytz>=2024.1
# pdfplumber (Kuvera PDFs) is installed by setup-oracle.sh in a separate pip
# pass: its pdfminer pin conflicts with casparser's in a single resolve.
# Sequence: pip install pdfplumber==0.11.9
#           pip install --no-deps --force-reinstall pdfminer.six==20240706
# NOTE: no kiteconnect — Kite REST is called directly via aiohttp (kite_sync.py)
# T8 swing sleeve only (lazy-imported; core bot runs without them):
yfinance>=0.2.40
pandas>=2.0.0
