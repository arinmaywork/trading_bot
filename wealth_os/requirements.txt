# Wealth OS — lean runtime (no ML/tick stack needed)
aiohttp>=3.9.0
casparser==0.8.1
pdfminer.six==20240706   # casparser's hard pin — pdfplumber tolerates it
pdfplumber==0.11.9       # Kuvera holding-statement PDFs (verified with pin above)
pytz>=2024.1
# NOTE: no kiteconnect — Kite REST is called directly via aiohttp (kite_sync.py)
# T8 swing sleeve only (lazy-imported; core bot runs without them):
yfinance>=0.2.40
pandas>=2.0.0
